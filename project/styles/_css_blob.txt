/* TatvaPractice — Brand tokens (per master brief) */
@import url('https://fonts.googleapis.com/css2?family=Mulish:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap');
:root {
  --tp-blue-50:#EEEEFF;--tp-blue-100:#D8D8FA;--tp-blue-200:#B5B4F2;--tp-blue-300:#8E8DE8;--tp-blue-400:#6C6BDE;
  --tp-blue-500:#4B4AD5;--tp-blue-600:#3C3BB5;--tp-blue-700:#2E2D96;--tp-blue-800:#212077;--tp-blue-900:#161558;
  --tp-violet-50:#FAF5FE;--tp-violet-500:#A461D8;--tp-violet-700:#703A9E;--tp-violet-900:#3E1C64;
  --tp-amber-50:#FFFBEB;--tp-amber-500:#F5B832;--tp-amber-700:#B47D10;
  --tp-slate-0:#FFFFFF;--tp-slate-50:#FAFAFB;--tp-slate-100:#F1F1F5;--tp-slate-200:#E2E2EA;--tp-slate-300:#D0D5DD;
  --tp-slate-500:#717179;--tp-slate-600:#545460;--tp-slate-700:#454551;--tp-slate-800:#2C2C35;--tp-slate-900:#171725;
  --tp-success:#10B981;--tp-warning:#F59E0B;--tp-error:#E11D48;
  --tp-r-12:12px;--tp-r-20:20px;--tp-r-24:24px;--tp-r-42:42px;
  --tp-shadow-sm:0 1px 3px rgba(23,23,37,.08);
  --tp-shadow-md:0 4px 8px -2px rgba(23,23,37,.08);
  --tp-shadow-lg:0 12px 24px -4px rgba(23,23,37,.10);
  --tp-shadow-xl:0 20px 40px -8px rgba(23,23,37,.14);
  --font-display:'Mulish',system-ui,sans-serif;
  --font-body:'Inter',system-ui,sans-serif;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{font-family:var(--font-body);background:#fff;color:var(--tp-slate-700);font-size:16px;line-height:24px;-webkit-font-smoothing:antialiased}
h1,h2,h3,h4,h5,h6{font-family:var(--font-display);color:var(--tp-slate-900);margin:0;letter-spacing:-.02em}
a{color:inherit;text-decoration:none}
img{max-width:100%;display:block}

/* Layout */
.tp-wrap{max-width:1240px;margin:0 auto;padding:0 24px}
.tp-section{padding:96px 0}
.tp-section-tight{padding:64px 0}
@media(max-width:768px){.tp-section{padding:56px 0}.tp-section-tight{padding:40px 0}}

/* Type */
.tp-display-xl{font-size:clamp(40px,5.6vw,56px);line-height:1.14;letter-spacing:-.03em;font-weight:800}
.tp-display{font-size:clamp(36px,4.6vw,48px);line-height:1.17;letter-spacing:-.02em;font-weight:700}
.tp-h1{font-size:36px;line-height:44px;font-weight:700;letter-spacing:-.02em}
.tp-h2{font-size:30px;line-height:38px;font-weight:700;letter-spacing:-.01em}
.tp-h3{font-size:24px;line-height:32px;font-weight:600}
.tp-h4{font-size:20px;line-height:28px;font-weight:600}
.tp-h5{font-size:16px;line-height:24px;font-weight:600}
.tp-body-xl{font-size:20px;line-height:32px;color:var(--tp-slate-600)}
.tp-body-lg{font-size:18px;line-height:28px;color:var(--tp-slate-600)}
.tp-body{font-size:16px;line-height:24px;color:var(--tp-slate-700)}
.tp-body-sm{font-size:14px;line-height:20px;color:var(--tp-slate-600)}
.tp-meta{font-size:13px;line-height:18px;color:var(--tp-slate-500)}
.tp-eyebrow{display:inline-flex;align-items:center;gap:8px;font-family:var(--font-display);font-size:13px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;color:var(--tp-blue-600)}
.tp-eyebrow::before{content:"";width:18px;height:1px;background:currentColor;opacity:.7}

/* Buttons */
.btn{display:inline-flex;align-items:center;gap:6px;font-family:var(--font-display);font-weight:600;border-radius:var(--tp-r-12);border:1px solid transparent;cursor:pointer;transition:all .15s;text-decoration:none;white-space:nowrap}
.btn-s{height:36px;padding:0 14px;font-size:13px}
.btn-m{height:42px;padding:0 16px;font-size:14px}
.btn-l{height:48px;padding:0 22px;font-size:15px}
.btn-solid{background:var(--tp-blue-500);color:#fff;box-shadow:var(--tp-shadow-sm)}
.btn-solid:hover{background:var(--tp-blue-600)}
.btn-solid:focus-visible{outline:2px solid var(--tp-blue-300);outline-offset:2px}
.btn-outline{background:#fff;color:var(--tp-blue-600);border-color:var(--tp-blue-200)}
.btn-outline:hover{background:var(--tp-blue-50);border-color:var(--tp-blue-500)}
.btn-ghost{background:transparent;color:var(--tp-slate-700)}
.btn-ghost:hover{background:var(--tp-slate-100);color:var(--tp-slate-900)}
.btn-link{color:var(--tp-blue-600);font-weight:600;display:inline-flex;align-items:center;gap:4px}
.btn-link:hover{color:var(--tp-blue-700)}
.btn-on-dark-solid{background:#fff;color:var(--tp-blue-700)}
.btn-on-dark-solid:hover{background:#fff;color:var(--tp-blue-800)}
.btn-on-dark-outline{background:transparent;color:#fff;border-color:rgba(255,255,255,.4)}
.btn-on-dark-outline:hover{background:rgba(255,255,255,.1)}
.arr{display:inline-block;transition:transform .2s}
.btn:hover .arr{transform:translateX(3px)}

/* Card */
.card{background:#fff;border:1px solid var(--tp-slate-200);border-radius:var(--tp-r-20);padding:28px;box-shadow:var(--tp-shadow-sm)}
.card-soft{background:var(--tp-slate-50);border:1px solid var(--tp-slate-200);border-radius:var(--tp-r-20);padding:28px}
.card-blue{background:var(--tp-blue-50);border:1px solid var(--tp-blue-100);border-radius:var(--tp-r-20);padding:28px}

/* Badges */
.badge{display:inline-flex;align-items:center;gap:6px;height:26px;padding:0 10px;border-radius:6px;font-size:12px;font-weight:500;font-family:var(--font-body)}
.badge-blue{background:var(--tp-blue-50);color:var(--tp-blue-700)}
.badge-violet{background:var(--tp-violet-50);color:var(--tp-violet-700)}
.badge-amber{background:var(--tp-amber-50);color:var(--tp-amber-700)}
.badge-success{background:#ECFDF5;color:#047857}
.chip{display:inline-flex;align-items:center;gap:6px;height:32px;padding:0 14px;border-radius:var(--tp-r-42);background:#fff;border:1px solid var(--tp-slate-200);font-size:13px;color:var(--tp-slate-700)}
.chip-blue{background:var(--tp-blue-50);border-color:var(--tp-blue-100);color:var(--tp-blue-700)}

/* Compliance badges (vivid, named) */
.compliance{display:inline-flex;align-items:center;gap:8px;padding:8px 14px;border-radius:10px;font-family:var(--font-display);font-weight:600;font-size:13px;border:1px solid}
.compliance-nha{background:#EEF2FF;color:#3730A3;border-color:#C7D2FE}
.compliance-abdm{background:#ECFDF5;color:#065F46;border-color:#A7F3D0}
.compliance-abha{background:#FEF3C7;color:#92400E;border-color:#FDE68A}
.compliance-iso{background:#F1F5F9;color:#1E293B;border-color:#CBD5E1}
.compliance-dpdpa{background:#FCE7F3;color:#9D174D;border-color:#FBCFE8}
.compliance-hipaa{background:#E0E7FF;color:#3730A3;border-color:#C7D2FE}
.compliance .dot{width:8px;height:8px;border-radius:50%;background:currentColor}

/* Top nav */
.nav{position:sticky;top:0;z-index:50;background:rgba(255,255,255,.92);backdrop-filter:blur(12px);border-bottom:1px solid var(--tp-slate-200)}
.nav-inner{display:flex;align-items:center;gap:40px;height:72px}
.logo{display:flex;align-items:center;gap:10px;font-family:var(--font-display);font-weight:700;font-size:18px;color:var(--tp-slate-900)}
.logo-mark{width:32px;height:32px;border-radius:10px;background:var(--tp-blue-500);color:#fff;display:grid;place-items:center;font-weight:700;font-size:14px;box-shadow:var(--tp-shadow-sm)}
.nav-list{list-style:none;margin:0;padding:0;display:flex;gap:28px}
.nav-item{position:relative}
.nav-link{display:inline-flex;align-items:center;gap:4px;padding:10px 0;font-size:14px;color:var(--tp-slate-600);cursor:pointer;font-weight:500}
.nav-link:hover,.nav-link.active{color:var(--tp-slate-900)}
.nav-link .chev{font-size:10px;opacity:.6;transition:transform .15s}
.nav-item:hover .chev{transform:rotate(180deg)}
.dropdown{position:absolute;top:calc(100% - 4px);left:-16px;min-width:340px;background:#fff;border:1px solid var(--tp-slate-200);border-radius:16px;box-shadow:var(--tp-shadow-lg);padding:10px;opacity:0;visibility:hidden;transform:translateY(-4px);transition:all .15s;z-index:60}
.nav-item:hover .dropdown{opacity:1;visibility:visible;transform:translateY(0)}
.dropdown a{display:grid;grid-template-columns:36px 1fr;gap:12px;padding:12px;border-radius:10px;align-items:flex-start}
.dropdown a:hover{background:var(--tp-slate-50)}
.dd-ico{width:36px;height:36px;border-radius:8px;background:var(--tp-blue-50);color:var(--tp-blue-600);display:grid;place-items:center;font-weight:700;font-size:14px}
.dd-title{font-family:var(--font-display);font-weight:600;font-size:14px;color:var(--tp-slate-900);line-height:1.3}
.dd-desc{font-size:12px;color:var(--tp-slate-500);margin-top:2px;line-height:1.4}
.nav-cta{margin-left:auto;display:flex;gap:10px;align-items:center}

/* Placeholder visual */
.ph{position:relative;border-radius:24px;border:1px solid var(--tp-slate-200);background:linear-gradient(135deg,var(--tp-slate-50),#fff);box-shadow:var(--tp-shadow-lg);overflow:hidden;display:grid;place-items:center;color:var(--tp-slate-500)}
.ph-label{position:absolute;top:12px;left:12px;font-family:var(--font-body);font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:var(--tp-slate-500);background:#fff;padding:4px 8px;border-radius:6px;border:1px solid var(--tp-slate-200)}
.ph-aspect-16-10{aspect-ratio:16/10}
.ph-aspect-4-3{aspect-ratio:4/3}
.ph-aspect-1-1{aspect-ratio:1/1}
.ph-doc{aspect-ratio:1/1;border-radius:50%;background:linear-gradient(135deg,var(--tp-blue-50),var(--tp-violet-50));border:1px solid var(--tp-slate-200);display:grid;place-items:center;color:var(--tp-blue-700);font-family:var(--font-display);font-weight:700}

/* Pre-footer CTA band — the ONLY heavy gradient */
.cta-band{position:relative;overflow:hidden;border-radius:24px;background:linear-gradient(135deg,var(--tp-blue-600),var(--tp-blue-800));color:#fff;padding:80px 56px;text-align:center}
.cta-band::before{content:"";position:absolute;inset:0;background:radial-gradient(500px 300px at 90% 0%,rgba(164,97,216,.35),transparent 60%),radial-gradient(400px 250px at 0% 100%,rgba(75,74,213,.35),transparent 60%);pointer-events:none}
.cta-band > *{position:relative;z-index:1}
.cta-band h2{color:#fff}

/* Sticky mobile CTA */
.mobile-cta{display:none}
@media(max-width:768px){
  .nav-list{display:none}
  .mobile-cta{display:flex;position:fixed;bottom:16px;right:16px;z-index:80;box-shadow:var(--tp-shadow-xl)}
}

/* Form */
.field{display:flex;flex-direction:column;gap:6px;margin-bottom:14px}
.field label{font-size:13px;font-weight:500;color:var(--tp-slate-700)}
.field input,.field select,.field textarea{height:42px;padding:0 12px;background:#fff;color:var(--tp-slate-900);border:1px solid var(--tp-slate-300);border-radius:10px;font-size:14px;font-family:var(--font-body);box-shadow:0 1px 2px rgba(23,23,37,.04)}
.field textarea{height:auto;padding:10px 12px;resize:vertical}
.field input:focus,.field select:focus,.field textarea:focus{outline:none;border-color:var(--tp-blue-500);box-shadow:0 0 0 3px var(--tp-blue-100)}
.req{color:var(--tp-error)}

/* Footer */
.footer{background:var(--tp-slate-900);color:var(--tp-slate-300);padding:80px 0 32px}
.footer-grid{display:grid;grid-template-columns:2fr 1fr 1fr 1fr 1fr;gap:48px;padding-bottom:48px;border-bottom:1px solid #2C2C35}
.footer .logo{color:#fff}
.footer h5{color:#fff;font-size:14px;font-weight:600;margin:0 0 14px}
.footer ul{list-style:none;margin:0;padding:0;font-size:14px}
.footer ul li{padding:6px 0}
.footer ul a{color:var(--tp-slate-300)}
.footer ul a:hover{color:#fff}
.footer-foot{padding-top:24px;display:flex;justify-content:space-between;font-family:var(--font-body);font-size:12px;color:var(--tp-slate-500);flex-wrap:wrap;gap:12px}
@media(max-width:768px){.footer-grid{grid-template-columns:1fr 1fr}}

/* FAQ */
.faq details{border-top:1px solid var(--tp-slate-200);padding:20px 0}
.faq details:last-of-type{border-bottom:1px solid var(--tp-slate-200)}
.faq summary{list-style:none;cursor:pointer;display:flex;justify-content:space-between;align-items:center;font-family:var(--font-display);font-weight:600;color:var(--tp-slate-900);font-size:18px}
.faq summary::-webkit-details-marker{display:none}
.faq summary span.plus{font-size:24px;color:var(--tp-slate-400);font-weight:400;transition:transform .15s}
.faq details[open] summary span.plus{transform:rotate(45deg)}
.faq details p{margin:14px 0 0;color:var(--tp-slate-600);max-width:60ch}

/* Bullets */
.checks{list-style:none;padding:0;margin:16px 0}
.checks li{display:grid;grid-template-columns:24px 1fr;gap:8px;padding:8px 0;color:var(--tp-slate-700);font-size:15px;line-height:1.5}
.checks li::before{content:"✓";display:grid;place-items:center;width:20px;height:20px;border-radius:50%;background:var(--tp-blue-50);color:var(--tp-blue-600);font-size:11px;font-weight:700;margin-top:2px}

/* Steps */
.steps{display:grid;grid-template-columns:repeat(4,1fr);gap:16px}
.step{position:relative;padding:24px;background:#fff;border:1px solid var(--tp-slate-200);border-radius:16px}
.step .step-n{font-family:var(--font-display);font-weight:800;color:var(--tp-blue-500);font-size:14px;letter-spacing:.06em}
.step h4{margin:8px 0 6px}
@media(max-width:768px){.steps{grid-template-columns:1fr}}

/* Grids */
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.grid-4{display:grid;grid-template-columns:repeat(4,1fr);gap:16px}
.grid-6{display:grid;grid-template-columns:repeat(6,1fr);gap:12px}
@media(max-width:1024px){.grid-4{grid-template-columns:repeat(2,1fr)}.grid-6{grid-template-columns:repeat(3,1fr)}}
@media(max-width:768px){.grid-2,.grid-3,.grid-4,.grid-6{grid-template-columns:1fr}}

/* Subtle blue tint section */
.section-tint{background:var(--tp-slate-50)}

/* Stats */
.stat{display:flex;flex-direction:column;gap:8px}
.stat-num{font-family:var(--font-display);font-weight:800;font-size:48px;line-height:1;letter-spacing:-.02em;color:var(--tp-blue-500)}
.stat-lbl{font-size:14px;color:var(--tp-slate-600)}

/* Testimonial */
.tcard{background:#fff;border:1px solid var(--tp-slate-200);border-radius:20px;padding:32px;display:flex;flex-direction:column;gap:24px;box-shadow:var(--tp-shadow-sm)}
.tcard blockquote{margin:0;font-family:var(--font-display);font-weight:500;font-size:18px;line-height:1.5;color:var(--tp-slate-800)}
.tcard .who{display:flex;gap:12px;align-items:center;margin-top:auto}
.tcard .who-meta .name{font-family:var(--font-display);font-weight:600;color:var(--tp-slate-900);font-size:14px}
.tcard .who-meta .role{font-size:13px;color:var(--tp-slate-500);margin-top:2px}

/* Specialty chip cloud */
.cloud{display:flex;flex-wrap:wrap;gap:8px}
